# Dockerfile para consumo moderado de CPU y RAM usando BusyBox
FROM busybox:latest

# Ejecutar un proceso de carga ligera en un bucle infinito
CMD ["sh", "-c", "while true; do { cat /dev/urandom | head -c 1M > /dev/null; } 2>/dev/null; sleep 10; done"]
