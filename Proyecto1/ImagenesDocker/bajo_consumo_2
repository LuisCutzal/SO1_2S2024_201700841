# Dockerfile para extremadamente bajo consumo de CPU y RAM usando BusyBox
FROM busybox:latest

# Comando para no hacer nada y minimizar el uso de recursos sin generar logs
CMD ["sh", "-c", "while true; do sleep 1 > /dev/null 2>&1; done"]
