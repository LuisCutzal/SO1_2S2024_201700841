# Dockerfile para otro contenedor de alto consumo de recursos
FROM ubuntu:latest

RUN apt-get update && apt-get install -y stress-ng

# Comando para alto consumo indefinido
CMD ["stress-ng", "--cpu", "4", "--vm", "2", "--vm-bytes", "512M", "--hdd", "1", "--timeout", "0"]

